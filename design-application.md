# 今回のデザイン適用方針

対象：HUG+ BEAUTY nano silica FACIAL CLEANSER 紹介サイト（**テスト用**）
採用スタイル：案B デイリー・ルーティン（slug なし／style-vault 未設置のため、design-system の範囲内で新規に設計）
ゴール：購入してもらう
読者：SNS経由で初めてこの商品を知った見込み客（スマホ閲覧が中心）

---

## 守る点（design-system の固定基準）

崩すとブランドから外れるため、今回の好みより優先する。

- **角丸なし**（`--radius-none:0`）。ボタン・画像・区切りブロックすべて直角
- **影は基本なし**。ボタンのホバー時だけ `0 4px 8px rgba(0,0,0,.2)`
- **色は navy + orange の2系統のみ**。オレンジ `#f2780e` は行動を促す場所（購入ボタン）だけに使う
- **teal `#00ac97` / pink `#f4b2ba` は使わない**。これは消費者向けショップ専用の色で、コーポレート側に出すと系統が混ざる
- **背景色は1ページに2色まで**（白 `#ffffff` と `#f5f6f7`）
- **書体**：本文・UIは Noto Sans JP、見出しは明朝（Shippori Mincho B1 / Zen Old Mincho）
- **行間は1.4**。長文の本文だけ1.8まで緩める
- **絵文字を使わない**。記号は `→` と `↗` のみ
- **文体は丁寧語（です・ます）**。命令形の煽り表現を使わない
- **ロゴは改変しない**。`credee-logo.webp` をそのまま、`object-fit: contain` で使う
- **グラデーション・グレイン・ぼかしを使わない**。写真は四角に切り抜いて置く

## 取り入れる点（案B デイリー・ルーティンから）

- 写真の面積を大きく取り、**写真が先・文章が後**の順で並べる
- 「成分の説明」より先に「**どんなときに、どう使うか**」を見せる
- セクションを短く区切り、スマホで**スクロールしながら理解できる**流れにする
- 購入ボタンをヒーローと最下部の**2か所**に置く（探さなくても押せる位置に常にある状態にする）
- 文章は1文を短くし、専門用語には必ず言い換えを添える

## 今回は使わない点（理由つき）

- **ブランドSVGイラスト（`assets/illustrations/`）** … 塗りの定義が欠けており、真っ黒な図形として表示されるため（design-system の readme に既知の問題として記載あり）。代わりに数字と文字で見せる
- **メガメニュー／ドロワー** … 1ページ構成なので不要。ナビは同一ページ内リンクのみ
- **人物写真（`person-*.webp`）** … 今回の商品と関係のない人物のため。肖像の扱いで誤解を生む
- **角丸16pxのやわらかい見せ方** … 案Bの「親しみやすさ」に合うが、design-system の「角丸なし」が優先。親しみは**写真の大きさと余白、文章のやわらかさ**で出す

## 採用する値

| 項目 | 採用値 |
|---|---|
| 色 | 主 `#14315c` ／ 文字 `#14315c` ／ 行動を促す色 `#f2780e` ／ 背景 `#ffffff`（交互セクション `#f5f6f7`） |
| 文字 | 大見出し Shippori Mincho B1 700 / 40px（スマホ28px）｜中見出し Zen Old Mincho 400 / 36px（スマホ24px）｜本文 Noto Sans JP 400 / 16px（スマホ15px）｜行間 1.4（本文の長文のみ1.8） |
| 余白 | 刻み 8px系（4・8・12・16・20・24・30・40・60）／セクション上下 60px（スマホ40px）／左右 40px（スマホ12px）／最大幅 1200px |
| 形 | 角丸 0px ／ 枠線 1px `#14293b33` ／ 影なし（ボタンhoverのみ `0 4px 8px rgba(0,0,0,.2)`） |
| 写真 | 高明度・青みのある白・クリニカル（研究／製造／製品）。加工なし、四角の切り抜き。全幅はヒーローのみ |
| 動き | ホバー：色と影のみ 300ms ease-in-out ／ スクロール表示：フェード＋8px上昇 400ms `cubic-bezier(.4,.4,0,1)` ／ それ以外は動かさない |

---

## 表現上の制限（健康・美容商材）

洗顔料は化粧品にあたるため、**効能効果を書ける範囲が法律で決まっている**（薬機法）。
このサイトでは次を守る。

- 書かない：「シミが消える」「美白」「毛穴がなくなる」「アンチエイジング」などの改善・治療を思わせる表現
- 書かない：ビフォーアフター、使用後の肌状態を示す写真・図
- 書ける範囲：「肌を清浄にする」「うるおいを与える」など、化粧品の効能効果の範囲内の記述
- 数値・実績・お客様の声は**渡された事実のみ**。今回は未提供のため `【要確認】` を表示して空欄にする

## 画像の扱い

`OPENAI_API_KEY` が未設定のため、今回は**画像生成を行わない**。
ユーザー選択に従い、design-system の既存写真を流用する。実際に開いて中身を確認済み。

| 使う画像 | 実際に写っているもの | 用途 |
|---|---|---|
| `hero-silica-dissolve.webp` | ほぼ白い背景に、青灰色の水の繊維と分子図 | ヒーロー背景 |
| `oem-cosmetics.webp` | 白い化粧品容器（チューブ・ボトル・ジャー）の集合 | 商品イメージ |
| `technology.webp` | 防塵服の研究員が青い手袋で透明な溶液を確認 | 原料・研究 |
| `factory.webp` | 研究室でピペットを使う作業（試験管立て） | 品質試験 |
| `evidence.webp` | PETボトルの充填ライン | 自社製造 |

**商品そのものの写真は素材にない**ため、`oem-cosmetics.webp` を代役に使い、該当箇所に `【要確認：実際の商品写真】` を表示する。

---

## 実装後に判明したこと（記録）

1. **素材のファイル名と中身が一致していない** … `evidence.webp` は実際にはPETボトルの充填ライン、`factory.webp` は研究室のピペット作業だった。alt文と見出しは実際に写っているものに合わせた。
2. **商品写真の解像度が足りない** … 代役に使った `oem-cosmetics.webp` は330×330pxで、PC幅では約1.5倍に引き伸ばされて表示される。実際の商品写真は**横1000px以上**で用意することを推奨。
3. **スクロール表示の作りを変更** … 当初「最初は透明→表示」で作ったが、JavaScriptが動かない環境で本文が読めなくなるため、「最初から表示→JSが動く環境でだけ隠して出す」方式に変更した。
4. **リンクの当たり判定を拡大** … ヘッダーとフッターのリンクが高さ21pxしかなく、指で押しづらかったため44pxに調整した。
5. **キーボード操作時の枠線を追加** … design-system では未定義（readme に課題として記載あり）だったため、このサイトで補った。
