Single English word in mincho that opens a section — the site's only "eyebrow" device.

```jsx
<SectionLabel>Technology</SectionLabel>
```

Title case, 40px, navy, centred. Japanese never appears in this slot; the Japanese headline follows underneath.
